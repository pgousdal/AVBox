# M0 acceptance

`make check` covers package, typed configuration, validated YAML registry, scanner classes, platform/historical models, hashing, rights/repair invariants, job state machine, verdicts, adapter and preservation boundaries, scanner-free CLI/API/UI, Debian Ansible, systemd hardening, tests, lint, typing and build.

M0 defers scanner installation, worker implementations, upload, cleanup orchestration, historical VMs/emulators, live RAB transport, authentication and LAN exposure.

